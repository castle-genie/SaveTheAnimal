<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.comment.dao.FcommentDAO">
	<resultMap type="fcomment" id="fcommentMap">
		<id column="fcomment_id" property="fcommentId"/>
		<result column="fcomment_content" property="fcommentContent"/>
		<result column="fcomment_date" property="fcommentDate"/>
		<result column="fboard_id" property="fboardId"/>
		<result column="user_id" property="userId"/>
	</resultMap>
	
	<!-- 자유게시판 댓글 작성 -->
	<!--  
	<insert id="FcommentInsert">
		INSERT INTO Fcomment (fcomment_content, fcomment_date, fboard_id, user_id)
		VALUES (#{fcommentConent}, sysdate , #{fboardId}, ${userId})
	</insert>
	-->
	<!-- 자유게시판 댓글 조회 -->
	<!--  
	<select id="FcommentList" parameterType="int" resultMap="fcommentMap">
		SELECT
				fcomment_id, fcomment_content, fcomment_date, fboard_id, user_id
		FROM 	Fcomment
		WHERE 	fcomment_id = #{fcommentId}
	</select>
	-->
	<!-- 자유게시판 댓글 작성 -->
	<!--  
	<insert id="FcommentInsert" parameterType="com.spring.comment.vo.FcommentVO">
		INSERT INTO Fcomment (fcomment_content, fcomment_date, fboard_id, user_id)
		VALUES (#{fcommentConent}, sysdate , #{fboardId}, ${userId})
	</insert>
	-->
	<!-- 자유게시판 댓글 수정 -->
	<!--  
	<update id="FcommentModify" parameterType="com.spring.comment.vo.FcommentVO">
		UPDATE Fcomment
		SET fcomment_content = #{fcommentContent}, user_id = #{userId}
		WHERE fcomment_id = #{fcommentId} and fboard_id = #{fboardId}  
	</update>
	-->
	<!-- 자유게시판 댓글 삭제 -->
	<!--
	<delete id="FcommentDelete" parameterType="com.spring.comment.vo.FcommentVO">
		DELETE
		FROM Fcomment
		WHERE fcomment_id = #{fcommentId} and fboard_id = #{fboardId}
	</delete>
	-->
</mapper>